const locationSchema = new Schema({
    name: String,
    address: String,
    city: String,
    state: String, 
    zip: Number,
    latitude: Number,
    longitude: Number,
    distance:Number
 });

